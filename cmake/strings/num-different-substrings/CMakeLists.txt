cmake_minimum_required (VERSION 3.4)

project (num-different-substrings)

include(num-different-substrings.cmake)

SET_PROPERTY(TARGET num-different-substrings PROPERTY FOLDER "num-different-substrings")
